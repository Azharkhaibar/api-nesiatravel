import { Request, Response } from "express";

export function getKaryawan(req: Request, res: Response) {
  res.send([]);
}

export function getKaryawanById(req: Request, res: Response) {
  res.send([]); 
}

export function createKaryawan(req: Request, res: Response) {
    res.send({})
}
